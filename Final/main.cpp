#include "final.h"

using namespace std;

int main(int argc,char* argv[]){
    int NSegs = 5;
	// int K = 2;
	// float percent = 0.7;
	// int* segs_capacity = new int[NSegs];
	// for(int i = 0;i < NSegs;i++)segs_capacity[i] = i;
	// Attica attica(NSegs,K,percent,segs_capacity);

    return 0;
}